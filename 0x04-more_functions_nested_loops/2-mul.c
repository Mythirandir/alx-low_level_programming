#include "main.h"

/**
 * main - check the code
 * mul: We are using this identifier to multiply 2 numbers
 * a: first integer for multiplication
 * b: second integer for multiplication
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int result;
	result = a * b;
	return (result);
}
